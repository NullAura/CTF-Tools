# CTF Tools operation registry draft.
# This file is the machine-readable source of truth for CLI and GUI discovery.

schema_version = 1

[[operations]]
id = "base64.decode"
name_zh = "Base64 解码"
name_en = "Base64 Decode"
category = "codecs.base"
aliases = ["b64", "base64decode", "base64解码"]
input = ["text", "file"]
output = ["text", "bytes"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "base64.encode"
name_zh = "Base64 编码"
name_en = "Base64 Encode"
category = "codecs.base"
aliases = ["b64", "base64encode", "base64编码"]
input = ["text", "bytes", "file"]
output = ["text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "url.decode"
name_zh = "URL 解码"
name_en = "URL Decode"
category = "codecs.web"
aliases = ["urldecode", "percent decode", "url解码"]
input = ["text"]
output = ["text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "url.encode"
name_zh = "URL 编码"
name_en = "URL Encode"
category = "codecs.web"
aliases = ["urlencode", "percent encode", "url编码"]
input = ["text"]
output = ["text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "hash.md5"
name_zh = "MD5"
name_en = "MD5"
category = "crypto.hash"
aliases = ["md5sum"]
input = ["text", "bytes", "file"]
output = ["text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "hash.sha256"
name_zh = "SHA256"
name_en = "SHA256"
category = "crypto.hash"
aliases = ["sha2", "sha-256", "sha256sum"]
input = ["text", "bytes", "file"]
output = ["text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P0"

[[operations]]
id = "xor.single_byte_bruteforce"
name_zh = "单字节 XOR 爆破"
name_en = "Single-byte XOR Bruteforce"
category = "crypto.xor"
aliases = ["xor brute", "xor爆破"]
input = ["text", "bytes", "file"]
output = ["ranked_text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P1"

[[operations]]
id = "http.raw.parse"
name_zh = "HTTP 请求包解析"
name_en = "Parse Raw HTTP Request"
category = "web.http"
aliases = ["raw request", "burp request", "请求包解析"]
input = ["text", "file"]
output = ["json"]
backend = "rust"
safety = "sensitive"
deterministic = true
batchable = true
priority = "P1"

[operations.secrets]
redact_by_default = true
fields = ["cookie", "authorization", "x-api-key", "token", "set-cookie"]

[[operations]]
id = "http.raw.to_python_requests"
name_zh = "请求包转 Python requests"
name_en = "Raw HTTP to Python requests"
category = "web.http.codegen"
aliases = ["request to python", "burp to python", "请求包转python"]
input = ["text", "file"]
output = ["code.python"]
backend = "rust"
safety = "sensitive"
deterministic = true
batchable = true
priority = "P1"

[operations.secrets]
redact_by_default = true
fields = ["cookie", "authorization", "x-api-key", "token", "set-cookie"]

[[operations]]
id = "http.raw.to_curl"
name_zh = "请求包转 curl"
name_en = "Raw HTTP to curl"
category = "web.http.codegen"
aliases = ["request to curl", "burp to curl", "请求包转curl"]
input = ["text", "file"]
output = ["code.shell"]
backend = "rust"
safety = "sensitive"
deterministic = true
batchable = true
priority = "P1"

[operations.secrets]
redact_by_default = true
fields = ["cookie", "authorization", "x-api-key", "token", "set-cookie"]

[[operations]]
id = "jwt.decode"
name_zh = "JWT 解析"
name_en = "JWT Decode"
category = "web.token"
aliases = ["jwt parse", "jwt解析"]
input = ["text"]
output = ["json", "text"]
backend = "rust"
safety = "sensitive"
deterministic = true
batchable = true
priority = "P1"

[operations.secrets]
redact_by_default = true
fields = ["jwt", "token", "signature"]

[[operations]]
id = "assets.classify"
name_zh = "资产分拣"
name_en = "Asset Classify"
category = "web.assets"
aliases = ["资产提取", "url提取", "ip提取", "domain extract"]
input = ["text", "file"]
output = ["json", "text"]
backend = "rust"
safety = "safe"
deterministic = true
batchable = true
priority = "P1"

[[operations]]
id = "pcap.http.extract"
name_zh = "PCAP HTTP 提取"
name_en = "Extract HTTP from PCAP"
category = "forensics.pcap"
aliases = ["pcap extract", "http objects"]
input = ["file"]
output = ["artifact_dir", "json"]
backend = "python_worker"
safety = "local_only"
deterministic = true
batchable = true
priority = "P2"

